
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sweet Treats Bakery</title>
  <style>
    /* Basic reset styles */
    body {
      margin: 0;
      padding: 0;
      font-family: sans-serif;
    }

    /* Header styles */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 2rem;
      background-color: #f0f0f0; /* Light gray background */
    }

    .logo {
      font-size: 1.5rem;
      font-weight: bold;
    }

    .navigation {
      list-style: none;
      margin: 0;
      padding: 0;
    }

    .navigation li {
      display: inline-block;
      margin-right: 1rem;
    }

    .navigation a {
      text-decoration: none;
      color: #333; /* Dark gray text */
    }

    .navigation a:hover {
      color: #000; /* Black text on hover */
    }

    /* Main content styles */
    .main-content {
      padding: 2rem;
    }

    h2 {
      margin-bottom: 1rem;
      font-size: 1.2rem;
    }

    p {
      line-height: 1.5;
    }

    /* Product list styles */
    .product-list {
      margin: 0;
      padding: 0;
      list-style: none;
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      justify-content: center;
      align-items: center;
    }

    .product-item {
      width: calc(30% - 1rem); /* 33% width for each product with some margin */
      margin-bottom: 1rem;
      border-radius: 5px;
      padding: 1rem;
      border: 1px solid #ccc;
      padding: 10px;
      box-sizing: border-box;
      shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      min-height: 584px;
      min-width: 333px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .product-item img {
      width: 100%;
      height: 300px; /* Set a fixed height for product images */
      object-fit: cover; /* Crop images to fit within the container */
    }

    .product-item h4 {
      margin-top: 0.5rem;
      margin-bottom: 0.2rem;
    }

    .product-item p {
      margin-bottom: 0.5rem;
    }

    .price {
      font-weight: bold;
    }

    /* Footer styles */
    .footer {
      text-align: center;
      padding: 1rem;
      background-color: #f0f0f0; /* Light gray background */
    }

    /* Responsive design adjustments (for smaller screens) */
    @media (max-width: 768px) {
      .product-item {
        width: calc(50% - 1rem); /* Adjust width for smaller screens */
      }
    }

    @media (max-width: 480px) {
      .product-item {
        width: 100%; /* Make products full width on very small screens */
      }
      .navigation li {
        display: block; /* Stack navigation items vertically on very small screens */
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      margin: 0;
      padding: 0;
      background-color: #f9f9f9;
    }
    header {
      background-color: #ffcc80;
      padding: 10px;
      text-align: center;
    }
    header h1 {
      margin: 0;
      font-size: 2.5em;
    }
    main {
      max-width: 800px;
      margin: 20px auto;
      padding: 20px;
      background-color: #ffffff;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      border-radius: 5px;
    }
    main h2 {
      font-size: 2em;
      margin-top: 0;
    }
    main p {
      font-size: 1.2em;
    }
    main a {
      display: block;
      margin-bottom: 15px;
      padding: 10px 15px;
      background-color: #ff9800;
      color: #ffffff;
      text-decoration: none;
      text-align: center;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    }
    main a:hover {
      background-color: #f57c00;
    }
    main ul {
      list-style-type: none;
      padding: 0;
    }
    main li {
      border-bottom: 1px solid #ddd;
      padding: 15px 0;
    }
    main li:last-child {
      border-bottom: none;
    }
    footer {
      text-align: center;
      padding: 10px;
      background-color: #ffcc80;
      position: fixed;
      bottom: 0;
      width: 100%;
    }

    /* Responsive Styles */
    @media only screen and (max-width: 600px) {
      header h1 {
        font-size: 2em;
      }
      main {
        padding: 10px;
      }
      main h2 {
        font-size: 1.5em;
      }
      main p {
        font-size: 1em;
      }
      main a {
        padding: 8px 12px;
      }
    }
  </style>
</head>
<body>
  <header style="text-align:center;">
    <h1>Sweet Treats Bakery</h1>
  </header>
  <main>
      <div style="text-align: center;">
    <h2>Welcome to Sweet Treats!</h2>
    <p>We offer a variety of delicious pastries, cakes, and breads.</p>

    <a href="/products/create" style="background-color: #ff9800; color: #ffffff; padding: 10px 15px; border-radius: 5px; text-decoration: none;">
      Add a New Produc:
  </a>

</div>
    <% if (products.length > 0) { %>
      <h3 style="margin-left : 1rem;">Our Products</h3>
      <ul class="product-list">
        <% products.forEach(function(product) { %>
          <li class="product-item">
            <img src="<%= product.image %>" alt="<%= product.name %>" style="" >
            <h4><%= product.name %></h4>
            <p><%= product.description %></p>
            <p class="price">Price: $<%= product.price %></p>
          </li>
        <% }) %>
      </ul>
    <% } else { %>
      <p>No products available yet.</p>
    <% } %>
  </main>
  <footer style="text-align:center;">
      <hr />
    <p>&copy; 2024 Sweet Treats Bakery</p>
  </footer>
</body>
</html>
